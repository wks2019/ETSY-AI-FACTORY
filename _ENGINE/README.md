# _ENGINE

Reusable PDF generation engines.

| File | Purpose |
|---|---|
| `planner_engine.py` | WeasyPrint-based planner generator (Product 1 baseline) |

Engines are product-agnostic. Product-specific config lives in `products/<id>/`.
